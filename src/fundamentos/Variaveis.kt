package fundamentos

fun main(args: Array<String>)
{
    // aqui declaramos a variável e atribuímos o tipo Int(numero inteiro) a ela.
    var a: Int

    /*
        Já nesse caso, a partir do tipo de dado atribuído à variável, o compilador
        infere e aribui o tipo à variável
    **/
    var b = 2

    // tda variavel em kotlin tem um tipo fixo

    a = 10

    print(a+b)
}