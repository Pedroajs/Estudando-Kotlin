package operadores

fun main() {
    var a: Int = 7
    var b: Int = 3

    b +=a // atribuicao aditiva
    b -= 4 // atribuicao subtrativa
    b *= 2 // atribuicao multiplicativa
    b /=2 // atribuicao divisiva
    b %=2 // atribuicao de modulo

    print(b)
}